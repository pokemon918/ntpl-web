import React from 'react';
import FormSection from '.';

export default {
	title: 'UI Kit / Forms / Form Section',
	component: FormSection,
};

export const normal = () => (
	<FormSection title="Form Section Example">Form Section Content</FormSection>
);
