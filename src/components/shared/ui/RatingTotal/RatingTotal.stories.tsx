import React from 'react';

import RatingTotal from './index';

export default {
	title: 'UI Kit / General / Rating Total',
	component: RatingTotal,
};

export const normal = () => <RatingTotal value={90} />;
